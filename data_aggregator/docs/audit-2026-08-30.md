# Data-quality audit — 30 Aug 2026, 04:20–05:00 BST (lane Q1)

What the live site (https://www.nepalfloodtracker.com) and the DERIVED tables actually showed, checked against the
sources, and what was fixed at the source. Read top to bottom; "fix" names the commit or the owner action.

## Summary

| # | What | Where | Verdict | Fix |
|---|---|---|---|---|
| 1 | "Latest" showed 12+ undated Gorkhapatra tag-page articles with "—" times, newest real headlines pushed out | home §08, `v_articles_recent` | **wrong ordering**: the view sorted by `coalesce(published_at, fetched_at)`, so anything without a date and fetched last floated to the top | migration `009_articles_recent_order.sql` (dated first, undated last, `fetched_at` exposed) — applied; Q1 commit |
| 2 | 207 articles had no `published_at`; 40 Kathmandu Post + 3 others carried the date in their URL path | `articles` (`outlet_rss_set`, `outlet_tag_pages`) | **parser gap**: RSS items without `pubDate` were stored undated even when `/2026/08/29/` was in the link | `_rss.published_from_path` shared by the feed and tag-page normalisers (`entry_datetime(e, link)`), test `test_rss_dates.py`; 43 existing rows backfilled from their URL (source-derived, not invented); Annapurna Post (27) and Gorkhapatra tag pages stay undated by design |
| 3 | Stat "4 numbers … from 2,362 (Nepal Police) to 14,599 (OPMCM portal)" | `stats.missing_counts_divergence`, home §02 | **misleading**: OPMCM `lost_open` is a queue of lost-person *reports* (duplicates, resolved-but-open), not a count of people | `stats.MISSING_CANDIDATES` drops OPMCM and adds Setu (NDRRMA) `missing`; caption now "from 1,275 (Setu (NDRRMA)) to 2,498 (NDRRMA)" after the next process run; test updated |
| 4 | `tuth_kathmandu`: expected 5, confirmed 8, unknown 0 | `place_status`, `/places/tuth_kathmandu` | **arithmetic**: confirmed can exceed expected (hospital admission list); the row read "8 of 5" | `ledger.reconcile_counts()` raises expected to confirmed; test `test_ledger_counts.py`; row corrects on the next process run |
| 5 | Side-by-side: NDRRMA 675 / 2,498 / 7,514 · Police (via press) 669 / 2,362 / 2,301 · MoFA 626 / 511 (foreigners) / 121 (foreigners found) · DoT "—" / "—" / 261 (NTB via press) · OPMCM "—" / 14,599 (open reports) / 2,473 (found) | home §04 | **matches sources** (NDRRMA sitrep #8, Khabarhub/KP/Annapurna quotes, MoFA 29 Aug brief, Radio Nepal, OPMCM `/api/stats`) | none — but see W1 for the OPMCM "missing" cell |
| 6 | `Nepal Police (UDB)`: `bodies_recorded` 593 (30 pages) vs `bodies_recorded_sum_of_districts` 113 "7 fetched districts"; `found_recorded` 0 (0 pages) | `figures_latest` | **partial fetch**: district pages fail with `SSLError` intermittently (run.log 02:39), so the district sum is not a national total (the note says so); `found` list genuinely empty for the date filter | none here — lane P5 is moving these sub-fetches into the pool; the web column uses the press figure first, so nothing wrong is shown |
| 7 | NDRRMA `rescued` 7,514 (sitrep) vs `rescued_portal` 6,633 vs `rescued_named` 2,189 | `figures_latest` | **correct, labelled**: three different NDRRMA surfaces; the sitrep total is the headline, the others are noted | none |
| 8 | Wikipedia figures (675 / 2,498 / 1,473 injured) | `figures_latest` | **not cited on the site** (`do not cite` note; no web mapping) | none |
| 9 | Gauges: the five live corridor stations appear twice (`bipad-*` and `dhm:*` ids, same names) | `v_gauges_latest` | **duplicate ids, consistent values** (BIPAD mirrors DHM); web tiles match by name and take the freshest, `gauges_alive` counts by `CORRIDOR_GAUGES` names (4 of 11) | none now; dedupe on `(station_name)` in `figures_latest`-style view if a page ever lists all stations |
| 10 | Rasuwagadhi last reading 26 Aug 02:55 UTC, Syabrubesi 03:05, Betrawati 03:35, Malekhu 05:55 — all `alive=false`; Dhunche/Galchi/Kali Khola/Devghat alive with 02:15–02:35 readings | `v_gauges_latest` | **matches the DHM record** (the seeded event timeline uses the same last readings) | none |
| 11 | `Trishuli at Kali Khola (Rainfall)` dead since 2021 | `v_gauges_latest` | rainfall station, excluded by the web tile regex | none |
| 12 | Articles: 60 newest titles reviewed — all flood-related; no phone numbers; names appear only as public headlines (e.g. a cremation notice) | `v_articles_recent` | **gate holds**; outlet_tag_pages logged "44 listing links failed the relevance gate" (working) | none |
| 13 | Google News items link to `news.google.com/rss/articles/…` redirect URLs | `articles` | cosmetic: the reader lands via Google; publisher shown correctly | queue: resolve the final URL at pull time (cheap HEAD) — not done |
| 14 | `places`: 79 ledger rows (38 settlements, 10 districts, 8 tunnel portals, 7 hospitals …); districts labelled `district` and split out of the places table; no future `last_contact_at`; Rasuwa district 3,185 expected / 1,029 confirmed | `v_place_status_latest`, `/places` | **consistent** apart from #4 | none |
| 15 | Sources: 51 registered, 0 failing on the last pull; `gdelt_monitor` timed out once (backoff engaged); `ntc_restoration_articles` never "fetched" because it is derived from the RSS sources (`url: (derived …)`) | `v_sources_status`, `_state.json`, `run.log` | **healthy**; the NTC row is a false "never fetched" on `/sources` | W2 |
| 16 | `DHM weather_warning_level` = 3 with note "Generally cloudy conditions will prevail" | `figures_latest` | plausible (level from the bulletin's warning table, note from its summary line) — not verified against the PDF | queue: check `dhm_weather` mapping when the next bulletin lands |

## Needs the web lane (not touched by Q1)

- **W1** — OPMCM column "missing" cell shows `lost_open` (14,599) with the "open reports ·" prefix. Consider labelling the
  cell "lost-person reports" outright, or showing `lost_open` on its own row; the number is a queue, not people.
- **W2** — `/sources` shows `ntc_restoration_articles` as never fetched; it is derived from the RSS sources. Either hide
  derived sources from the freshness table or show the parent's timestamp.
- **W3** — "Latest" renders each title twice in the markup (visible + a duplicate, probably an aria/sr-only copy); harmless
  but doubles the text for screen readers.
- **W4** — `stats.towers_restored_pct` (73%) exists but is not in `STAT_CARDS`.

## Needs the owner

- Nothing blocking. Key rotation and Full Disk Access notes stand (see `docs/runbook.md` §0).

## Method

`figures_latest` (national) listed publisher × metric with value / as_of / url and compared with the source pages
quoted in `note`; `stats` captions read against values; the 60 newest `v_articles_recent` rows scanned for relevance
and a phone-number regex; `v_gauges_latest` checked for negatives, duplicates and stale-but-alive; `v_place_status_latest`
checked for unknown > expected, confirmed > expected, future contact times, districts among places; `v_sources_status`,
`pipeline/_state.json` failure counters and the last three `run.log` runs read for errors; live pages fetched with curl.
All tests: 234 passing after the fixes.

# Decisions log

Dated, append-only. One line per decision: what was decided, the alternative considered, where it shows up. Newer entries at the bottom of each day. When a decision is reversed, add a new line that says so; do not edit the old one.

Template — copy, fill, append:

```
- **D-NNN · <short title>.** Decided: … Alternative considered: … Where: `path/or/table`. Who: <name>.
```

Sources for the seed entries: `PLAN.md` (§0 principles, §2 zones, §12 open items), the approved build plan's "Decisions confirmed with the user", and what the migrations `db/migrations/001–005` actually encode.

## 2026-08-29

- **D-001 · The database is the single source of truth.** Decided: scraped, contributed and computed data all live in Supabase; the website is a portal, the scripts are readers and writers; nothing exists only on a laptop or only on the site. Alternative: files on disk + static site rebuilds. Where: `PLAN.md` §0. Who: Aryaa.
- **D-002 · Two scripts, one cron line.** Decided: `pull_external_data` (external → RAW) and `process_data` (RAW + ARCHIVE → DERIVED), run back-to-back by `pipeline/run.sh`. Alternative: one monolithic script; per-source cron entries. Where: `pipeline/`. Who: Aryaa.
- **D-003 · Three zones enforced by RLS, not application code.** Decided: ARCHIVE (PII, owner + service) / RAW (anonymised, service) / DERIVED (public, computed); the anon key ships in the browser and can only do what `004_rls.sql` allows; the website has no privileged code path. Alternative: server actions with a secret key. Where: `db/migrations/004_rls.sql`, `db/docs/05-rls.md`. Who: Aryaa.
- **D-004 · Supabase as database only.** Decided: Postgres + RLS + one Realtime publication + two Storage buckets; no edge functions, no Supabase cron, one guard trigger. Alternative: edge functions for anonymisation. Where: `db/docs/06-realtime-and-storage.md`. Who: Aryaa.
- **D-005 · Users contribute raw; the pipeline anonymises.** Decided: submissions stored verbatim in `reports_archive`; `process_data` ⓪ projects new rows into `reports_anon`; the website never calls OpenAI. Alternative: anonymise in the browser or on submit. Where: `db/docs/02-archive.md`. Who: Aryaa.
- **D-006 · Anonymous Supabase auth is the user identity.** Decided: `signInAnonymously()` on first visit gives a stable `auth.uid()`; `/me` is `select … where user_id = auth.uid()`; optional upgrade to email/phone later without changing the id. Alternative: cookie-only ids; accounts. Where: `users`, `web/docs/04-auth-and-identity.md`. Who: Aryaa.
- **D-007 · One text box, chips not fields.** Decided: the box is the only required field; chips insert prompt lines; extraction populates the schema. Alternative: a structured multi-field form (Google Form / Kobo). Where: `PLAN.md` §7, `/report`. Who: Aryaa.
- **D-008 · Three languages, route-based.** Decided: `/{en|ne|hi}` routes so a forwarded link carries its language; NE and HI written by the builder, complete at launch, reviewed by the team afterwards. Alternative: cookie-based locale; Chinese at launch. Where: `web/messages/`, `web/middleware.ts`. Who: Aryaa.
- **D-009 · Raw pulls in Postgres, binaries in Storage.** Decided (closes `PLAN.md` §12.1): text bodies in `raw_pulls.body`, PDFs/images in the private `raw` bucket with `storage_path`. Alternative: everything in Storage. Where: `001_archive.sql`, `005_realtime_storage.sql`. Who: Aryaa.
- **D-010 · Age as a band in RAW, exact only in ARCHIVE.** Decided (closes §12.2): `reports_anon.age_band` ∈ {0-17, 18-39, 40-64, 65+}. Alternative: exact age with k-anonymity checks. Where: `002_raw.sql`. Who: Aryaa.
- **D-011 · Human dedup queue as a table; review tool open.** Decided: ambiguous merges (0.6–0.9) land in `dedup_queue` with `decision/decided_by/decided_at`; reviewed from the Supabase table editor for now. Alternative: an `/admin` route behind a password. Where: `003_derived.sql`. Status: tool choice still open (§12.3). Who: Aryaa.
- **D-012 · 3D corridor is procedural, not a DEM.** Decided (supersedes §12.4's pre-baked mesh): `corridor-3d.js` from the design ported verbatim into a React island, live markers from `v_place_status_latest`, PNG fallback on WebGL failure or slow connections. Alternative: Copernicus DEM tiles pre-baked into the repo. Where: `web/components/three/`. Who: Aryaa (confirmed).
- **D-013 · Contact address.** Decided (closes §12.5): `contact@nepalfloodtracker.com` on `/about`. Alternative: `hello@`. Where: `web/app/[lang]/about`. Who: Aryaa. **Revoked 30 Aug (D-069): the mailbox was never created.**
- **D-014 · Free tier; Presence degrades first.** Decided: stay on the Supabase free tier; if the Realtime connection cap is hit, "people here now" hides itself and the contribution counters remain (polling `v_live_counts`). Alternative: Pro plan from day one. Where: `web/docs/09-live-scoreboard.md`, `docs/runbook.md` §5. Who: Aryaa (confirmed).
- **D-015 · Withdraw is soft and one-way for the user.** Decided: owner sets `withdrawn_at`; trigger forces `status='withdrawn'` and reverts any other change; row retained; excluded from processing/counts within one cadence; no authorities-export command in this build. Alternative: hard delete; full edit rights. Where: `004_rls.sql` trigger, `db/docs/05-rls.md`. Who: Aryaa (confirmed).
- **D-016 · OpenAI `gpt-4o-mini` with structured outputs, $20 guard.** Decided: extraction/anonymisation/place resolution use `gpt-4o-mini`; `lib/llm.py` tracks spend in `_state.json` and stops at `OPENAI_BUDGET_USD`. Alternative: a larger model with a smaller row cap. Where: `pipeline/docs/process_data/07-llm-budget.md`. Who: Aryaa (confirmed).
- **D-017 · Cadence: every 4 h tonight, every 15 min from distribution.** Decided: `0 */4 * * *` tonight; `*/15 * * * *` once distribution starts; `PULL_INTERVAL_MINUTES` in `pipeline/lib/config.py` and `web/lib/config.ts` drives the copy and the stale threshold and must change with the crontab. Alternative: 15 min from the start. Where: `docs/runbook.md` §1. Who: Aryaa (confirmed).
- **D-018 · Migrations via the Management API query endpoint.** Decided: `db/apply.py` posts each file to `POST /v1/projects/{ref}/database/query` using `SUPABASE_ACCESS_TOKEN` or the Supabase CLI's keychain token; applied files recorded with a checksum in `_migrations`; an edited applied migration is refused. Alternative: `supabase db push` (needs the DB password and a linked project); the dashboard SQL editor (no ledger). Where: `db/apply.py`, `db/docs/07-applying-migrations.md`. Who: Aryaa.
- **D-019 · Public reads of RAW only through owner-privilege views.** Decided: `v_articles_recent`, `v_gauges_latest`, `v_place_status_latest`, `v_sources_status`, `v_live_counts` are granted to `anon`; `figures`, `articles`, `pulls` tables stay service-only. Alternative: `security_invoker` views + per-table policies. Where: `003_derived.sql`, `004_rls.sql`. Who: Aryaa.
- **D-020 · Every number carries publisher, `as_of` and `url`.** Decided: `figures` has no path for a bare figure; the site shows five publishers side by side and explains why they differ rather than picking one. Alternative: a single "best" number. Where: `figures`, `figures_latest`, home §03. Who: Aryaa.
- **D-021 · Deploy only on code change; ISR 5 min.** Decided: `vercel --prod` when the app changes; data reaches the page through ISR (`revalidate = 300`) and Realtime, never a rebuild. Alternative: rebuild on every pipeline run. Where: `web/docs/12-deploy.md`. Who: Aryaa.
- **D-022 · Autonomy rule.** Decided: during the build the builder never stops to ask; ambiguities are resolved by the simplest choice consistent with the design and plan and logged here with the alternative. Where: this file. Who: Aryaa (confirmed).
- **D-023 · `docs/sources.md` is generated.** Decided: `docs/gen_sources_md.py` renders `sources.yaml` grouped by family, reusing `db/seed/gen_sources.py`'s group/reliability rules so the page and the `sources` table agree. Alternative: hand-maintained table. Where: `docs/gen_sources_md.py`. Who: docs lane.
- **D-024 · Registry `pii` seeds as boolean.** Decided: `sources.pii` is true only for registry values `true`/`mixed`; free-text notes ("some headlines name individuals") seed as false and remain visible in `docs/sources.md`; such sources still extract place/count/status only. Alternative: a three-state column. Where: `db/seed/gen_sources.py`. Who: docs lane (documented, not changed).

## Open

- §12.3 review tool for `dedup_queue` (D-011).
- Named Nepal-side co-owner with access to DERIVED and `findings` (`PLAN.md` §9 handoff) — record the name here when agreed.
- Wave-2 sources (tag pages, live blogs, Chinese search APIs, Wikipedia revisions, GDELT, geospatial catalogues, Setu, Police UDB, DAO lists) — each becomes a normaliser and a line here when it lands.

- **2026-08-30 01:30** — Deployed the full site to production (Vercel). Domain nepalfloodtracker.com (apex + www) attached to Vercel by the owner at Squarespace; a stale local resolver briefly made the apex look unconfigured (see runbook §7.0).

- **2026-08-30 02:30** — Incident: `vercel --prod` was run with the shell cwd still in `pipeline/`, which auto-created a Vercel project "pipeline" and uploaded that folder for ~10 minutes before it was deleted via the API (HTTP 204). Vercel CLI honours `.gitignore`, so `pipeline/.env` should not have been uploaded, but this cannot be proven after deletion. **Action for the owner in the morning: rotate the OpenAI key (platform.openai.com) and the Supabase service-role key (dashboard → Settings → API), then update `pipeline/.env`.** Guard added: `.vercelignore` with `*` in `data_aggregator/`, `db/`, `pipeline/`.

## 2026-08-30 (overnight build; times BST)

- **D-025 · News relevance gate with generic-place exclusion.** Decided (01:25, tightened 02:35): every RSS / search / live-blog item must match a flood keyword **and** a corridor-place alias before it becomes an `articles` row; district names and generic places (`kathmandu`, `bharatpur`, `pokhara_pahs`, the Kathmandu/Bharatpur hospitals) do not count on their own; `process_data.py --purge-irrelevant` drops stored rows that fail. Alternative: LLM classification per headline (cost, latency). Where: `pipeline/normalisers/_rss.py` (`is_relevant`, `GENERIC_PLACE_IDS`), `processing/purge_irrelevant.py`. Who: owner ("this doesn't seem particularly relevant to the flood" — KP robotics headline), builder.
- **D-026 · `/report` is one page.** Decided (01:05): the "Who are you?" cards and the box render on the same page, family preselected; two screens added friction. Alternative: the design's two-screen flow. Where: `web/components/form/ReportFlow.tsx`. Who: owner.
- **D-027 · Cadence tonight 240 min; scheduler is a detached loop with launchd as the successor.** Decided (00:50 → 03:05): `scripts/install_schedule.sh` installs both; the launchd agent never ran (`last exit code = 78: EX_CONFIG` — macOS TCC blocks `/bin/bash` from reading `~/Desktop` until Full Disk Access is granted), so a `nohup` loop started from the terminal runs `run.sh` every N minutes and launchd takes over when the owner grants access. Alternative: crontab (same TCC problem), a VM tonight. Where: `scripts/install_schedule.sh`, `docs/runbook.md` §1. Who: builder.
- **D-028 · `run.sh` takes a lock.** Decided (02:40): a lock directory (`pipeline/.run.lock`, stale after 3 h) makes an overlapping tick exit 0 — three concurrent `process_data` runs had tripled the log and the OpenAI calls. Alternative: `flock` (not on macOS by default). Where: `pipeline/run.sh`. Who: builder.
- **D-029 · Forecast metrics are named per day.** Decided (02:35): Open-Meteo flying-window quality is stored as `flying_window_quality:<YYYY-MM-DD>` so `figures_latest` (one row per publisher × metric × scope) keeps all three days instead of collapsing to the newest. Alternative: a `forecast` table. Where: `normalisers/openmeteo_corridor.py`, consumers in `ledger.py`/`stats.py`/web. Who: builder.
- **D-030 · Log redactor masks 9–14-digit numbers only.** Decided (02:00): the phone regex in `pipeline/lib/log.py` needs 9–14 digits and exempts ISO dates — it had been masking `day=2026-08-29` as `[phone]`. Where: `pipeline/lib/log.py`. Who: builder.
- **D-031 · District rows are not places.** Decided (02:00): `place_status.status_label = 'district'` for `places.kind = 'district'` and district-like ids; the site splits them into a "By district" table (`web/lib/places-split.ts`) instead of swamping the places table. Alternative: drop district rows from the ledger. Where: `processing/ledger.py`, `web/app/[lang]/places`. Who: builder.
- **D-032 · `last_contact_at` is observed-only.** Decided (02:10): the ledger writes the last *observed* contact from a place (entity contact, report time, place-scoped figure `as_of`, article mentioning the place, live gauge) and null when nothing dated exists — never the run time, which had made every place look "contacted just now". Where: `processing/ledger.py`, `docs/data-model.md`. Who: builder.
- **D-033 · Press-quoted counts become figures (step 3.5).** Decided (02:00 → 02:10): Police / Tourism / NTB numbers quoted in articles are lifted into `figures` with publishers `Nepal Police (via press)`, `Dept of Tourism (via press)`, `NTB (via press)` so the side-by-side columns fill even when the agency has no API; a fractional step number keeps ③b before ④ without renumbering. Alternative: leave those columns empty. Where: `processing/press_figures.py`, `docs/process_data/03b-press-figures.md`. Who: builder.
- **D-034 · Side-by-side columns accept several publisher spellings.** Decided (02:10): `web/lib/config.ts` `AGENCIES[].publishers` lists every spelling a column may draw from, tried in order (`Nepal Police`, `… (via press)`, `… (UDB)`; `OPMCM portal`; `NTB (via press)`), instead of forcing the pipeline to normalise publisher names and lose provenance. Where: `web/lib/config.ts`, `web/lib/queries.ts` `pickFigure`. Who: builder.
- **D-035 · `figure_series` (migration 007) for trends.** Decided (00:40): one public row per publisher × metric × scope × NPT day, written by step ⑨, so sparklines and "since yesterday" never read the private `figures` table. Alternative: a view over `figures` (would expose it). Where: `db/migrations/007_series.sql`, `processing/trends.py`. Who: builder.
- **D-036 · `.vercelignore` outside `web/`.** Decided (02:30, after the incident logged above): `*` in `data_aggregator/`, `db/`, `pipeline/` so a `vercel` run from the wrong cwd uploads nothing; the rule remains "deploy only from `web/`". Where: the three `.vercelignore` files, `docs/runbook.md` §7. Who: builder.
- **D-037 · Context compaction + auto-compact.** Recorded (02:05): the builder's context compacted; the owner enabled auto-compact; state lives in `PROGRESS.md` and the memory pointer so work resumes from the file, not from memory. Who: owner.
- **D-038 · Usage-limit pause.** Recorded (02:15 → 03:31): the session limit stopped all four cycle-4 lanes before they produced files; per the owner, work paused and resumed after the reset (03:20) with the same briefs. Where: `PROGRESS.md` cycle log. Who: owner.
- **D-039 · The 3D corridor becomes an interactive flood simulation — the cornerstone.** Decided (02:15) by the owner: the corridor is the first thing visitors see and must be an animated, interactive flood ("replay the night", sliders for lake volume / breach, drop houses / bridges / buses in the path and watch them be swept — Turbo-Dismount-style engagement), with the live statistics layered on afterwards; procedural terrain, pure-TypeScript water sim, no new dependencies, PNG fallback on reduced motion. Supersedes the static-drift part of D-012 (procedural terrain and live markers stand). Where: `web/docs/14-flood-sim.md`, `web/lib/flood-sim.ts`, `web/components/three/`. Who: owner.
- **D-040 · Flood-sim physics: virtual pipes on the terrain grid, no dependencies.** Decided (03:40): `web/lib/flood-sim.ts` is a pure-TypeScript shallow-water "virtual pipes" solver over a 192 × 104 grid of the design's terrain (plus a carved riverbed and a landslide dam upstream of the lakes), rendered as a second grid mesh that draws only wet triangles. Alternative considered: a GPU/shader height-field or a physics engine — rejected as new dependencies with no testability; the pure module is covered by vitest (mass conservation, front ordering, no NaN). Illustrative only, labelled on the page.
- **D-041 · The sim's clock is a lookup, not a prediction.** Decided (03:45): the clock chip maps the front's chainage onto the recorded arrivals in `event_timeline` (08:37 collapse … 13:00 Devghat). Alternative: sim-time × a scale factor — rejected because it would invent times. The lake-volume slider starts at the figure China's MWR published (2.0 Mm³).
- **D-042 · Real bridges on the path come from the HOT OSM survey.** Decided (04:15): `figures_latest` rows with metric `bridge_status` whose note starts "washed out" / "damaged" are joined to gazetteer chainage (one per place, max 10) and pre-placed as bridge objects the wave sweeps; counted separately from the visitor's objects. NESRA "to inspect" bridges are not placed (status unknown).
- **D-043 · Corridor canvas uses `touch-action: pan-y`.** Decided (04:25): a vertical swipe on the panel scrolls the page on phones; horizontal drags orbit. The design's `touch-action: none` trapped scrolling at the top of the home page.
- **D-044 · Adaptive sim quality.** Decided (04:25): after ~20 frames longer than 34 ms the scene drops to one substep and updates the water mesh every other frame; never re-raises within a session.
- **D-045 · Scoreboard "since last pull" suppresses the hydration warning.** Decided (04:15): the ISR-rendered label and the client's differ by design (time passes); `suppressHydrationWarning` on that cell, the 30 s tick keeps it honest. Alternative: client-only rendering — rejected (empty cell on first paint).
- **D-046 · A silent lane is stood down, not waited for.** Decided (04:50): lane U3 (web UX/trends) produced no commits or file changes in 75 minutes; it was told to stop and its list was re-issued to lane W4 with concrete items and no screenshot survey. Rule for the future: a lane must commit something within 30 minutes or report.
- **D-047 · Per-place "now" line (step ⑩) is template-first, model-polished.** Decided (04:40, lane P5): counts, publisher names and headline titles → a deterministic trilingual template, then gpt-4o-mini polish with a $1 per-run cap and a fallback to the template, so the column is never empty when signals exist and no PII reaches the model. Migration 008 adds `place_status.now_*`.
- **D-048 · Site copy must not promise an authorities channel that does not exist.** Decided (06:40): the design's copy said reports "go to Nepal Police and MoFA through an agreed channel"; there is no export tool in this build (owner's decision), so About, the report footnote/how-it-works/thanks and the /me privacy lines now say the full report is *kept* for the official channel the team is arranging and that nothing is passed on automatically. Re-word when the channel exists.
- **D-049 · The schedule is an explicit serial loop, not launchd/cron.** Decided (08:50, owner, on seeing the "bash — item from unidentified developer" login item): the launchd agent and the detached loop were removed; `pipeline/scheduler.py` runs pull → process, sleeps N hours, repeats, in a terminal the owner starts. Trade-off accepted: it does not survive logout/reboot; a VM/systemd unit later.
- **D-050 · Attachments are encouraged and private.** Decided (09:00, owner): the report form invites people to attach whatever helps (photos, video, voice notes, screenshots, documents) — a private `report-media` bucket with own-folder policies, 50 MB/file, 10 files, `report_files` rows in ARCHIVE; files are uploaded after the report row exists so a failed upload never loses the text; the pipeline never reads file contents; nothing is ever shown publicly. The form was decluttered to one column with "How it works" as a banner and Where/Contact as light optional rows.
- **D-051 · The home page opens with the visitor's own part.** Decided (09:30, owner): the first block under the header is "Your part" — this device's contribution count ("You haven't added anything yet") with the big Add button — because the ask has to be the first thing seen; the header slot that held "Add what you know" now holds Sources · About · Share so the sources and the sharing are one tap away on every page.
- **D-052 · Every source shows exactly what was extracted.** Decided (09:30, owner): `/sources` rows expand into the newest figures and headlines pulled from that source, read through public views (012) over the anonymised RAW tables; RAW itself stays denied to the anon key.
- **D-053 · Corridor v2 is modular with one ground truth.** Decided (10:00, from the owner's "nothing should fall into the mountain" and "robust video game feel"): the scene is split into terrain · water · markers · objects · camera modules behind contracts (`components/three/scene/types.ts`); everything samples one bilinear ground (`makeGroundSampler`); pieces of broken objects are integrated by `lib/flood-physics.ts` whose never-below-ground invariant is unit-tested and exposed as `debug().belowGround` (asserted 0 in e2e); the object catalogue has 14 non-sensitive kinds (no schools/temples; an office building instead); chip tap = immediate placement in the path with a pulsing marker; X-ray terrain scales with how horizontal the orbit is.
- **D-054 · The corridor is viewed from above, always.** Decided (10:15, owner): no chase camera — a first-time viewer must see the whole corridor; the overview looks down steeply enough that the river floor is visible between the walls; portrait phones look along the corridor. X-ray stays on by default at 0.3 (the owner likes seeing the flood through the mountain) and strengthens with tilt.
- **D-055 · Water is deep blue at the breach and browns as it scours.** Decided (10:25, owner): contrast against everything else, and the browning tells the story; foam only on the crest because the sim's speeds would otherwise whiten the whole sheet.
- **D-056 · No foundation pads.** Decided (10:30, owner, "random discs"): settlements stand on their own ground on the flattest nearby spot; the status colour lives on the roofs; the only ring left is the transient placement cue.
- **D-057 · Official channels show the disaster hotline 1234, not NEOC 1149.** Decided (11:20 BST 30 Aug, owner asked "is it 1234 now?" — verified): MoHA launched the toll-free disaster helpline **1234** on 11 July 2026 (Radio Nepal "Government introduces disaster hotline 1234"; myRepublica; Khabarhub "MoHA expands 1234 to all 77 districts"; moha.gov.np "विपद् हेल्पलाइन 1234"). 1149 was the old NEOC line and still appears on third-party pages (UN-SPIDER etc.), so a reader can see both — we show the current one. Changed `web/lib/config.ts` (label "Disaster hotline (NEOC)", tel 1234), the README/CONTRIBUTING/PLAN copies and the OfficialChannels comment; the Claude Design export under `design/` keeps 1149 as a historical reference. Police 100 and Red Cross 1130 unchanged.
- **D-058 · The "random discs" were the object foundation pads — gone; names are a small key with a toggle; a swept object is carried whole before it breaks.** Decided (12:30 BST 30 Aug, owner's screenshots): D-056 removed the settlement pads but every *object* (including the pre-placed real HOT-OSM bridges, which stand beside the villages) still had a pad + black ring in `scene/objects.ts` — removed; bridges now sit level on the higher abutment so the deck crosses above the river. Place pills shrink 1.9 → 1.1 units and a **Names** toggle (top-right, persisted per device) hides them so the damage is what you look at. Physics: the flood *takes* an object as one body (pivot at its centre, radius = half its height) that rides the crest for up to 6 s, tumbling, and breaks on its first hard hit; pieces inherit its velocity, are pulled back toward the channel (`CENTRE`) instead of beaching, follow the water faster (`FLOW_GAIN` 0.85, cap 12), and past the plate's open east edge they fly and fall (`FALL_FLOOR`) — the corridor ends in a waterfall, with spray going over the edge from `water.ts`. Anchored kinds (bridge, mast) still break where they stand. The story feed shows 3–6 rows by canvas height (5 under the canvas on phones). Traced locally: a house at Timure lifts at 3.6 s, rides 24 units, breaks at 9.7 s, and its wreckage is 55 units downstream by 25 s; `belowGround` stayed 0 throughout.
- **D-059 · The water is a level fill of the valley, and translucent.** Decided (13:00 BST 30 Aug, owner's side-view screenshot: "the water should mould to the same shape as the mountain, but translucent — more translucent to the side, less than the mountain"): the sheet's vertex height was bed + depth × 4.2, which lifts it off the bed and leaves its edges hanging in the air where real water would meet the walls. Now `lib/water-fill.ts` gives every cell the highest nearby surface level (6 cells, falling 0.25/cell so a flat plain does not flood forever) and a cell is drawn wet when that level stands above its own ground — the surface extends sideways exactly until it meets the terrain at its own height. Opacity 0.94 looking down → 0.58 from the side (the mountain goes 0.82 → 0.4), so objects under the wave show through; the mud stain now climbs the walls to the water line. Pure function, unit-tested (V-valley: rows under the level are wet, surface is level; falloff and radius bound the plain).
- **D-060 · Zoom goes toward the point under the cursor.** Decided (13:00, owner): wheel zoom and pinch zoom slide the orbit target toward the ground point under the cursor / pinch midpoint by the zoom factor (`zoomToward`, clamped to the corridor like a pan), so zooming into a part of the corridor lands on that part instead of the centre.
- **D-061 · Real footage sits directly under the simulation, curated in the repo.** Decided (13:30 BST 30 Aug, owner: "add some actual videos of the flood; place them below the flood simulation — this increases the viral potential"): nine YouTube clips (CCTV at Rasuwagadhi, NDTV's last minutes at Trishuli Bazar, CNA's bridge, a Timure/Syabrubesi eyewitness Short, NewsX aerial, Shilapatra, CNN-News18, Straits Times, Kantipur TV), each verified through YouTube's oEmbed (title + channel) and screened against the fact-check desks' lists of AI/recycled clips. The list is `web/lib/videos.ts` — a reviewed file, not a table or a form — because a fake clip would travel with the site's credibility; visitors' footage still arrives through the report form's attachments. Tiles are click-to-play posters (no iframe, no YouTube cookie until a tap; `youtube-nocookie.com`), with our own en/ne/hi captions that claim only what the upload says, a place chip into the place page, and the credit. The block ends with "Have footage from the corridor?" into the form. Home is still three things: the footage is part of the corridor's story, not a fourth thing (docs/17, docs/18).
- **D-062 · Breach defaults to slow; controls in two rows; "Flood volume", not "avalanche volume".** Decided (14:20 BST 30 Aug, owner): the run opens on the slow breach so the wave builds; row 1 is the volume slider, row 2 puts Breach and Drop-in-the-path on one line. The owner asked whether the slider should say "avalanche volume" since an avalanche caused the flood — the number is the barrier-lake volume China's MWR published (the avalanche breached that lake; its own volume is not what drives the wave), so the label became "Flood volume · the barrier lake the avalanche breached".
- **D-063 · Full device resolution on phones.** Decided (14:20, owner: "why is it so low resolution on mobile"): `setPixelRatio` was capped at 1.5 on phones — a 3× screen rendered at half its pixels. Now min(devicePixelRatio, 3) everywhere; the low-quality fallback trims substeps/spray, never resolution. `debug().pixelRatio` exposes it.
- **D-064 · Overview + Cinematic; footage under the panel, three clips.** Decided (14:20, owner): "Frame" is "Overview"; "Cinematic" restarts the run with the chase camera (opens on the lake, follows the front along the channel with a slow sway and bob, eases back to the overview when the run ends; any gesture or Overview cancels it). The footage sits directly under the simulation's controls as three featured clips (`FEATURED_VIDEOS`), not a separate section of nine. "My folder" is "My info" (nav, page title, sentences, en/ne/hi).
- **D-065 · The share message opens with the live numbers; WhatsApp goes through its composer; the OG card shows the corridor.** Decided (15:00 BST 30 Aug, owner: "the default sharing message must be very engaging … why is there no preview from the share link on mobile"): `share.hook` = "🌊 675 dead · 2,498 out of contact · 7,514 rescued. One page follows every village … watch the wave run 72 km, then add what you know — one message is enough:" with the NDRRMA figures from `/api/share-numbers` (same source as the OG card; fetched only when a share UI mounts; the plain text when unknown). The missing preview: iOS hands `navigator.share({text, url})` to WhatsApp as a *text* item, and WhatsApp only previews a URL item or a link typed into its composer — so every WhatsApp pill is `wa.me/?text=` (composer → preview + hook), and the device sheet is "More…" given the URL alone. The OG card gets the corridor picture (fallback PNG strip) under the three numbers with "▶ Watch the flood run 72 km".
- **D-066 · The three goals sit on the front page, in their own card.** Decided (15:00, revised 15:40, owner: "separate the goals from Your part; side by side"): on Home, Your part and "What this page does" are two cards — 3:2 side by side on desktop, stacked on phones — not on the sub-tabs' compact variant. 1 collect what people know · 2 put every number, place and headline in one place · 3 spread — "#3 is you →" with the Share button at the card's foot. About keeps the long version.
- **D-067 · Clips autoplay muted when scrolled into view.** Decided (15:55 BST 30 Aug, owner): a tile ≥ 60 % in view mounts the embed muted and looping with a "muted · tap the speaker" badge; out of view it returns to the poster (only what is on screen plays); a tap on the poster plays with sound and stays. No autoplay for reduced-motion or Save-Data visitors — browsers only allow silent autoplay anyway.
- **D-068 · The favicon is generated, not the scaffold's.** Noted (16:05 BST 30 Aug, owner: "why has the favicon not updated from the vercel default"): `app/favicon.ico` was still create-next-app's placeholder (the Vercel triangle) and Next links the .ico before `icon.svg`, so Safari/Chrome showed it. `npm run icons` now also writes `app/favicon.ico` as a PNG-in-ICO (16/32/48) from `app/icon.svg`; the .ico is never hand-edited.
- **D-069 · No contact email until a mailbox exists.** Decided (16:15 BST 30 Aug, owner: "we don't actually have that email"): every mention of `contact@nepalfloodtracker.com` removed (About, README, CONTRIBUTING, the gazetteer user-agent); `CONTACT_EMAIL` in `lib/config.ts` is `null`. Agencies and correctors are pointed at the report form's “Agency” path, which lands in the archive and is read. When a mailbox exists, set `CONTACT_EMAIL` and the About button can go back to `mailto:`.
- **D-070 · Contact mailbox is `nepalfloodrescuers@gmail.com`.** Decided (16:30 BST 30 Aug, owner): supersedes D-069 — the address goes back on About (mailto button, with the form's “Agency” path as the secondary link), README, CONTRIBUTING and the gazetteer user-agent, via `CONTACT_EMAIL` in `lib/config.ts`.
- **D-071 · A level water line wall to wall, and unmistakably blue.** Decided (16:45 BST 30 Aug, owner's low side view: "confusing; the water needs to be more blue and actually mould to the mountain visibly"): the level fill bulged over the channel (deep centre, shallow edges → a hump, read as a lump on the mountain) — now the first 4 cells of the fill carry no decay, so the surface is a level lake between the walls with the water line meeting them; foam cut to the steepest crests and high speeds, specular down, a blue emissive, opacity 0.97 → 0.67 from the side, and the blue (#144fb3) holds for 64 units before mud takes over on the plain.
- **D-072 · The run's tally, and what flashes.** Decided (17:10 BST 30 Aug, owner): the corridor's stat boxes are now a strip — villages reached n/N · people believed in its path (sum of `reported` over reached places) · real bridges lost · swept away with a per-kind breakdown (🏠1 🚌1). The box that changed is the one that flashes (a real bridge flashed the "swept away" box before, which read as "nothing happens"). "Share this run" is gone from the controls; Cinematic sits before Overview; the drop-in row is on its own line under the breach speed. Share text is shorter, no em dash, and the link sits after a blank line (WhatsApp, X, copy).
- **D-073 · `web/lib/tokens.ts` was never in git.** Found (17:00) while validating in a clean worktree: the root `.gitignore` rule `token*` (for credentials) matched the design-tokens file, so every deploy had been shipping it from the working tree and a fresh clone could not build (`@/lib/tokens` missing). Fixed with a negation and a force-add. Deploys now run from a clean worktree of `main` (`scratchpad/wt`) so uncommitted work in the checkout cannot reach production.
- **D-074 · Family intake is archive-only.** Decided (30 Aug, before public distribution): supersedes D-005 and every later assumption that questionnaire text is anonymised, matched, counted or published. The form still stores the original row/files, owner metadata and a separate activity event; `FAMILY_REPORT_PROCESSING_ENABLED=false` fails closed before any family-table/model access in every pipeline stage. Public outputs use public sources only. Active rows remain Received; Withdraw retains the private row/files but bars future review/processing/handoff. The unchanged schema and dormant legacy implementation are reserved for a future programme that must receive its own controller/receiver, consent, retention, UI and security review. Where: pipeline context/steps, report/My Info UX, architecture/runbook/security remediation docs. Who: Aryaa.
- **Doc renumbering note.** `pipeline/docs/process_data/` now has `03b-press-figures.md`, `07-digest.md`, `08-llm-budget.md`, `09-failure-modes.md`, `10-timeline-and-trends.md`; D-016's pointer `07-llm-budget.md` is today `08-llm-budget.md`.
